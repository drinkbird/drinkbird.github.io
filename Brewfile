# Run `brew bundle` from the repo root to install everything needed
# to develop this Jekyll site locally.

# Ruby 3.3 — matches what GitHub Pages itself builds with.
# Newer Ruby (3.4+/4.x) is rejected by the github-pages gem's deps.
brew "ruby@3.3"

# For the npm uglify script in package.json
brew "node"
